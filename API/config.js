var config = {
    //url to be used in link generation
    //mongodb connection settings
    mongodb: {
        host: '127.0.0.1',
        port: '27017',
        database: 'ADV_DB'
    },

    //server details
    server: {
        host: '127.0.0.1',
        port: '8080'
    }
}

module.exports = config;