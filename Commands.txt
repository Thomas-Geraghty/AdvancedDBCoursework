// Find crimes handled by regional police force.
// x = Police Force Region
db.getCollection("PoliceData").find(
    { 
        "Falls within" : x
    }
);

// Find crimes reported by regional police force.
// x = Police Force Region
db.getCollection("PoliceData").find(
    { 
        "Reported by" : x
    }
);